<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMESSI RICHIESTI -->

    <!-- Permesso chiave per richiedere di ignorare l'ottimizzazione della batteria -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Permessi per il servizio in foreground e notifiche -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Permesso per la registrazione audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Permesso per avviare il servizio al boot del dispositivo -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Permessi per la localizzazione -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Buona pratica aggiungerlo con FINE_LOCATION -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeLog"
        tools:targetApi="34">

        <!-- Activity di avvio (invisibile) -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity per l'onboarding -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- Activity per la richiesta dei permessi -->
        <!-- NOTA: Questa MainActivity sembra ridondante se il flusso è gestito da LauncherActivity -->
        <!-- Potrebbe essere un residuo di codice precedente. Per ora la lasciamo. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Activity per la dashboard principale -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Activity per le impostazioni -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Servizio di registrazione -->
        <service
            android:name=".AudioRecordingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Receiver per l'avvio del telefono -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>