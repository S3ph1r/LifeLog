<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================== PERMESSI ================== -->

    <!-- Permessi per la registrazione e il servizio in background -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Permessi per la localizzazione (in primo piano e in background) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Permesso per le notifiche (da Android 13 in su) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permesso per avviare il servizio al boot del dispositivo -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Permesso per disabilitare l'ottimizzazione della batteria -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Permesso per la connessione a Internet (per l'upload) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permesso per cambiare le impostazioni audio (necessario per attivare il Bluetooth SCO) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Permesso per connettersi ai dispositivi Bluetooth (necessario da Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeLog"
        tools:targetApi="34">

        <!-- ================== ACTIVITIES ================== -->

        <!-- Activity di avvio: invisibile, decide dove andare -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity per il flusso di configurazione iniziale -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- Activity per la dashboard principale dell'app -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Activity per le impostazioni -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- ================== SERVIZI E RECEIVER ================== -->

        <!-- Servizio principale per la registrazione audio -->
        <service
            android:name=".AudioRecordingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Receiver per riavviare il servizio al boot del dispositivo -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>